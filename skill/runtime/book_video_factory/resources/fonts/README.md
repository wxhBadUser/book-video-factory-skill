# 字体资产

- `SmileySans-Oblique.otf`：得意黑 v2.0.1，用作图书视频 V2 的中文标题字体。
- `SmileySans-OFL.txt`：对应的 SIL Open Font License 1.1。

正文当前暂用 macOS 自带 `Songti SC`，路径和字体索引均由 `config/video_style_v2.json` 配置。渲染器不会依赖字体家族名猜测，可在后续下载完成后直接替换为思源宋体静态 OTF。
